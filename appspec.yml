version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/mynameisviii.com
hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: ec2-user
permissions:
  - object: /var/www/mynameisviii.com
    mode: 644
    acls:
      - "d:u::rwx"
      - "d:g::rwx"
      - "d:o::rwx"
    owner: ec2-user
    type:
      - directory
