version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/mynameisviii.com
hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
permissions:
  - object: /var/www/mynameisviii.com
    pattern: "**/*"
    owner: ec2-user
    mode: 777
    type:
      - file
